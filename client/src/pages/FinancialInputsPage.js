export default function FinancialInputsPage() {
  return (
    <div style={{ padding: '32px 40px', maxWidth: 700 }}>
      <h1 style={{ margin: '0 0 8px', fontSize: 24, fontWeight: 700, color: 'var(--text)' }}>Financial Inputs</h1>
      <p style={{ margin: 0, color: 'var(--text-muted)', fontSize: 14 }}>
        Configure billing rates, revenue targets, and other financial parameters.
      </p>
      <div className="card" style={{ marginTop: 32, padding: 32, textAlign: 'center', color: 'var(--text-muted)' }}>
        Coming soon
      </div>
    </div>
  );
}
